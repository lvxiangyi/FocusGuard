# FocusGuard MVP 01

- **版本**：`v1.0.0`
- **Git tag**：`mvp-demo-v1.0.0`
- **日期**：2026-09-06
- **分支**：`mvp-cut`
- **入口**：直接用浏览器打开本目录 `index.html`，无需构建、联网或启动正式应用。

## 设计范围

三个入口：专注、我的案例、偏好设置。桌面采用深蓝侧栏、浅色工作区、蓝色计时环；窄屏自动纵向排列。

1. 输入任务，选择 25 / 45 / 60 分钟，开始、结束、到期结束。
2. 实时倒计时与示例 AI 判定；底部 DEMO 控件可触发连接失败、重试和全屏式拦截预览。
3. 通用单选题替代强制英译日；选择答案、错误反馈、正确后回到任务。
4. 最近记录与拦截页均可直接纠正判断，输入实际标签和原因，保存到个人案例；可查看、删除。
5. 界面语言和题目语言独立设置：中文、英文、日语、韩语、法语、葡萄牙语。题干、选项跟随题目语言，其余交互文案跟随界面语言。
6. 检查间隔、连续分心阈值收在折叠设置中，不干扰主流程。

## 演示说明

- 静态前端 demo，不连接后端，不进行真实截屏、AI 判定、题目生成或案例检索。状态、参考数、历史记录和题目为示例。
- 全屏式拦截只覆盖浏览器视口，可用退出按钮或 Escape 关闭；没有系统级锁屏。
- 纠正后直接返回任务用于展示交互，正式产品仍需定义异议复核策略。
- 界面偏好和案例保存在浏览器 localStorage（键 `focusguard.mvp-demo.v1`）；用户输入保留原文，不机器翻译。禁用存储时仍可体验，但刷新后不保留。
- 倒计时使用真实时间，弹窗打开期间暂停；刷新页面后回到未开始状态。
- 标注的画面为示意 UI，不是截屏。正式产品应以对应历史截图替换。
- 监督阈值仅保存偏好；演示中通过按钮触发拦截。AI 状态为固定样例，不随自定义任务生成。

## 文件

- `index.html`：语义化页面、弹窗和各功能入口。
- `styles.css`：视觉规范、桌面和移动布局、焦点与减少动画支持。
- `app.js`：六语文案、独立题目语言、交互状态和本地案例存储。

## 回溯

在独立工作目录查看：`git worktree add ../FocusGuard-mvp-demo mvp-demo-v1.0.0`。
本版本只新增 demo 和项目说明，不修改正式应用的前后端行为。
